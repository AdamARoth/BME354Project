void exportData()
{
  boolean continueExport = true;
  byte memAccessIndex = 7;
  while(continueExport)
  { 
    elapsedTime = (((memAccessIndex-1)/2)-3)*5; //Produces elapsed time in seconds
    setTemp =  EEPROM.read(memAccessIndex);
    currTemp = EEPROM.read(memAccessIndex+1);
    if(memAccessIndex+3 > 255)
    {
      continueExport = false;
    }
    else
    {  
      exportMATLAB();
    }
    memAccessIndex = memAccessIndex + 2;
  }
  elapsedTime = 0;
}
